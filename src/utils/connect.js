// import { PrismaClient } from '@prisma/client';
//
// let prisma;
//
// if (process.env.NODE_ENV === 'production') {
//     prisma = new PrismaClient({
//         // настройки для продакшн окружения
//     });
// } else {
//     prisma = new PrismaClient({
//         // настройки для других окружений (например, development)
//     });
// }
//
// export default prisma;